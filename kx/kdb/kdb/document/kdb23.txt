KDB 2.3

2001-11-13
 http		-N default is 200000 rows
 $insert	$insert into t(v's) table-expression
 groupby	unlimited. (FIX integer overflow)

2001-10-11
 parallel	optimize ... where date.month ...  etc.
 save't'	save binary table(t.l)
 http		-N default is 1000 rows
 funcs		list of functions

2001-09-16
 insert2	update old (discard new)
 -p		k db ... -p  kills the process on port p (easy restart)
 (null)floor	treats null as empty list

2001-08-31
 odbc		client exec returns null on updates
 within	... 	within(vector,vector)

2001-08-24	
 x floor y	last i where x[i]<=y
 insert		multi-column key +,-,min,max
 browser	where field=[null]

2001-07-16
 bcp.k		process nulls

2001-07-15
 odbc		fix (2001-05-29 host:port prompt)
 time within...	fix (time within(calculations))
 *.kdb		NT fix right-click display
 parallel	distributed. fix *.m
 current_database

2001-06-12
 par.txt	super partition directories, e.g. /k0 /k1 ...	
 *.kr		autoload

2001-05-29
 ipsert		{'t'|t} {union|+|-|min|max} u
 mins,maxs	prefix scans e.g. price-mins price
 x exp,x log	power and base
 timestamp	date+time
 current_...	current_time.hh etc.
 current_...	local(instead of gmt) date time timestamp

2001-05-17
 parallel	webserver: optimize display
 parallel	recognize date partitions

2001-04-02
 insert1	append: doesn't check the key
 'f'last	select last a,last b,last c,... by f from ...
 .odbc		views h;NUC exec returns tables
 empties	fix <x>sum <x>avg <x>deltas <x>ratios
 distinct	fix $select distinct ... order by f desc
 one seek	partition;sort;index
 parallel	round-robin partitions
 parallel	allow date partition directories e.g. 20010524
 load'dir'	automatically loads *.[stk]

2001-03-04
 ansisql	various fixes. allow unqualified field names in joins.
 fills		varchar
 -o		readonly

2001-01-23
 n floor	floor x/n, e.g. 5-minute rollup: ... by 300 floor hhmmss
 parallel	pass temporary globals to slaves
 parallel	allow uniform functions
 parallel	implies -s
  
2001-01-15 
 params		e.g. .d.r("select from ? where f in ?";(table;list))
 java/vb/c	NUC pass table parameters as (fields;data[;datetypes])
 cross		allow multicolumn cross

2000-12-20
 insert		speed up from 4 million to 8 million random updates per second
 odbc		fixes: vb5, access97, ...

2000-12-19
 batch		... ; ... ; ...
 jdbc		c.setAutoCommit(false);s.executeUpdate(s)...;c.commit();c.setAutoCommit(true);
 odbc		fixes: winsql3.5, access2000

2000-11-29
 ansisql	optimize constant joins (u.a=t.a and u.b=const)
 debug		report error file and line in nested load
 debug		JDBC reports errors (http://kx.com/a/kdb/connect/jdbc/jdbc.zip)
KDB 2.4

2002-09-30
 view		allow column views to variables outside table
 -p		kill p if no console
 upsert		upsert[,]
 fills		faster

2002-07-15
 n sums		moving sums

2002-07-02
 avg		coerces arg to float(also sumf)

2002-06-04
 sql		allow $select ... from(s union t)   non-standard sql92
 distinct	returns table (not keyed table) NUC

2002-05-07
 bcp		decimal
 table		table x will make a table from x(for export)
 in			FIX for sorted date. (... where date.month in ...)
 .xml?..	export record, list and atom(as well as table)

2002-03-11
 parallel	optimize n last(like n first)

2002-03-05
 table -	FIX(table-unkeyed table)

2002-02-15
 virtual	double virtual memory databases(1GB to 2GB)

2002-02-14
 sql92		access2000 order by expressions

2002-02-12
 odbc		access2000 (fix sqlgetdata of varchar)
 -S n		no contention (NUC: slave processes must be multiple of disk drives)
 sum		NUC: int:sum int (the way it used to be)
 bcp		NT load solaris. http://kx.com/a/kdb/connect/bcp.k 
 -l		k db x.t -l  (loads x.kdb(data);loads x.t(funcs and views);runs log)
		save'' only saves the data.   for debug: \.d.LF:1
